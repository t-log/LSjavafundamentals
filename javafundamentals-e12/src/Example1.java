public class Example1 {
    /*
    * anonymous blocks
    * don't use in real-world scenarios (not clean code)
    * 2 kinds of anonymous blocks : static and non-static
    * non-static block : executed in order(if there are many) and every time an instance is created
    * */
    public static void main(String[] args) {
        Cat.sayMeow();
    }
}