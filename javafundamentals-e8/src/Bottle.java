public class Bottle {
    final double volume;
    /*by initialising the value in the constructor just before the object is created
    * and stored in memory we can assign a value we require to the final instance variable
    * */
    Bottle(double volume){
        this.volume = volume;
    }
}
